<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hireledger.mapper.AddressMapper">

    <!-- ID로 주소 조회 -->
    <select id="findById" parameterType="Long" resultType="com.example.hireledger.domain.entity.Address">
        SELECT *
        FROM address
        WHERE id = #{id}
    </select>

    <!-- 주소 저장 -->
    <insert id="save"
            parameterType="com.example.hireledger.domain.entity.Address"
            useGeneratedKeys="true"
            keyProperty="id">

        INSERT INTO address (
        zipcode,
        address,
        address_detail
        ) VALUES (
        #{zipcode},
        #{address},
        #{addressDetail}
        )

    </insert>

</mapper>
