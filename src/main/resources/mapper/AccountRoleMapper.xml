<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hireledger.mapper.AccountRoleMapper">

    <!-- 특정 계정의 role_id 목록 조회 -->
    <select id="findRolesByAccountId" parameterType="long" resultType="long">
        SELECT role_id
        FROM account_role
        WHERE account_id = #{accountId}
    </select>

</mapper>
